import { LoginCard } from "@mr/components/features/auth/LoginCard";

export default function LoginPage() {
  return (
    <div className="relative flex min-h-screen items-center justify-center bg-gradient-to-br from-white to-blue-100/50">
      <LoginCard />
    </div>
  );
}
